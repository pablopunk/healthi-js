{
  "name": "healthi",
  "version": "1.0.8",
  "description": "Simple module to get your Macbook's battery health",
  "main": "bin/healthi.js",
  "scripts": {
    "build": "babel src --presets babel-preset-es2015 --out-dir bin",
    "prepublish": "yarn run build",
    "prepush": "yarn run test",
    "test": "yarn run build && standard src/*.js test/*.js && ava"
  },
  "keywords": [
    "health"
  ],
  "author": "Pablo Varela",
  "license": "MIT",
  "devDependencies": {
    "ava": "^0.19.0",
    "babel-cli": "^6.18.0",
    "babel-preset-es2015": "^6.18.0",
    "husky": "^0.13.3"
  },
  "dependencies": {
    "shelljs": "^0.7.6",
    "standard": "^10.0.2"
  },
  "repository": {
    "type": "git",
    "url": "https://github.com/pablopunk/healthi-js"
  }
}
